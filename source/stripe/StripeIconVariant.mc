import Toybox.Lang;

// Shared icon contrast mode constants (no Properties access).
// ON_LIGHT: artwork for light stripe backgrounds.
// ON_DARK: artwork for dark stripe backgrounds.
module StripeIconVariant {

    const ON_LIGHT = 0;
    const ON_DARK = 1;
}
