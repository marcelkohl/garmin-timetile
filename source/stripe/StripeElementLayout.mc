import Toybox.Graphics;

// Layout values shared by every stripe element.
module StripeElementLayout {

    const ELEMENT_WIDTH = 40;
    const ROW_HEIGHT = 16;
    const ELEMENT_HEIGHT = ROW_HEIGHT * 2;
    const ROW_PADDING = 2;
    const ROW_ICON_INSET = 2;
    const DEFAULT_ROW_FONT = Graphics.FONT_XTINY;

    const ROW_TOP = 0;
    const ROW_BOTTOM = 1;
}
